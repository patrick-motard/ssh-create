#!/usr/bin/env bash
dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"
mkdir -p $HOME/.local/bin/tools
cp $dir/ssh-create $HOME/.local/bin/tools/
cp $dir/ssh-remove $HOME/.local/bin/tools/
